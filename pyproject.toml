[project]
name = "sillytavern-mcp"
version = "0.1.0"
description = "SillyTavern MCP Server"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=3.3.1",
]

[project.scripts]
sillytavern-mcp = "sillytavern_mcp:main"
